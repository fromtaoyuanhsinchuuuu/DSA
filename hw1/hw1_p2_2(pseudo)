n = array.size
i = 1,j = 2,pairnum = 0
while j <= n:
    if array[i] == -1:
        i++ // 已經被跑過了
        continue
    if arr[j] == arr[i] * 2:

        /* 設為-1表已跑過,不會重複跑到 */
        arr[i] = -1
        arr[j] = -1
        i++
        /* pair! */
        pairnum++

    j++
if pair_num == n / 2: // 若對數剛好為n/2則每個a都有其對應的2a
    return 1
else:
    return 0
    


    
