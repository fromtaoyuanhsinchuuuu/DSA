empty_Queue q1,q2
i = 1
for i < arr.size - 1:
    q1.enqueue(arr[i + 1] - arr[i]) // put distance into q1

i = 1
for i <= m:
    ans = max(q1.head, q2.head) / 2 // 若有其中一個queue為空，則使ans = 不為空的queue.head
    q2.enqueue(ans)
    q2.enqueue(ans)
    if ans == q2.head:
        q2.dequeue
    else:
        q1.dequeue
return ans

    



