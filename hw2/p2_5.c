#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include <ctype.h>
#include <assert.h>
#include <stdlib.h>
#include <math.h>

#define MAXN 10000

int main(void)
{
    int n;
    scanf("%d", &n);

    int A[MAXN];
    for (int i = 0; i < n; i++){
        scanf("%d", &A[i]);
    }

    int k;
    scanf("%d", &k);
    
    return 0;
}
